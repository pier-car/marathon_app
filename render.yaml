services:
  - type: web
    name: marathon-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: APP_PIN
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.4"
